import React from "react";
import {} from "react-router-dom";

const styles = {
  fontFamily: "sans-serif",
  textAlign: "center"
};

export default () => <h1 styles={styles}>Magic happens in</h1>;
